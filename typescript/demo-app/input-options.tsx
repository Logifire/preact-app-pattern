import { Component, h } from "preact";

export class InputOptions extends Component {
    render() {
        return (
            <div>
                Input Options
            </div>
        );
    }
}